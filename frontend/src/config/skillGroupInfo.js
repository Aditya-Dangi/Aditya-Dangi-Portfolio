/**
 * Short, factual taglines per skill group — categorisation copy, not claims
 * about specific years of experience or project counts (the resume data
 * doesn't break down to that granularity, so the constellation's detail
 * panel stays honest about what it's showing: a map of the stack, not a
 * fabricated scorecard).
 */
export const skillGroupInfo = {
  Languages: "The languages code is written in, day to day.",
  Backend: "Services, security and data access, built with Spring.",
  Frontend: "Interfaces and state, built with Angular and RxJS.",
  "Databases & Tools": "Persistence, containers and the daily toolchain.",
  Engineering: "The fundamentals everything above is built on.",
};

export default skillGroupInfo;
